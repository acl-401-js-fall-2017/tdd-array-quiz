let squaredNum = [1, 4, 5, 7];

function forEach(squaredNum, function(ele) {
    return (ele * ele);
});

function filter(array, callback) {
    const filteredArr = [];

    for(let i = 0; i < array.length; i++) {

    }
}
module.exports = forEach