const assert = require('assert');

describe('take and array and return a new array with numbers that are squared and filtered') => {
    const createsFilteredArr = (num) =>{
        return item;
    };

    it('filter', () => {
        const initArr = [];
        const filteredArr = filter(initArr, createsFilteredArr);

        aseert.deepEqual(filteredArr, initArr[]);
    });
}
