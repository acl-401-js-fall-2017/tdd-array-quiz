const arrayNum = require('../lib/arrayNum');
const assert = require('assert');



let arrayNum = [];

describe('testing an array of numbers and filtering', () => {

    beforeEach(function() {
        arrayNum = new arrayNum;
    })

    it('should take an array of numbers, square them and filter to > 20', () => {
        const mappedArray = getAllObj.map((obj) => {
        return array;
        assert.deepEqual(array, [25, 49]);
    });


});
